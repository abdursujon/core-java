package EnumSet;

/**
 *   1. EnumSet.of(e1, e2, ...) — create set with specific enum constants
 *   2. EnumSet.allOf(Enum.class) — create set with all constants of the enum
 *   3. EnumSet.noneOf(Enum.class) — create empty set for that enum type
 *   4. EnumSet.range(from, to) — create set with range of constants (inclusive)
 *   5. EnumSet.complementOf(set) — create set with all constants NOT in the given set
 *   6. EnumSet.copyOf(collection) — create set from another collection
 *   7. set.add(val) — add element
 *   8. set.remove(val) — remove element
 *   9. set.contains(val) — check if element exists
 *   10. set.size() — number of elements
 *   11. set.isEmpty() — check if empty
 *   12. set.clear() — remove all elements
 *   13. set.addAll(collection) — union
 *   14. set.retainAll(collection) — intersection
 *   15. set.removeAll(collection) — difference
 *   16. set.forEach(lambda) — loop through elements
 *   17. set.iterator() — get an iterator
 *   18. set.stream() — convert to Stream
 */
public class EnumSets {
}