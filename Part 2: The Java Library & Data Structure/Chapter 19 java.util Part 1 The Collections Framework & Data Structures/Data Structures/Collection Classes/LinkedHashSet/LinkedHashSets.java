package LinkedHashSet;

/**
 *   1. set.add(val) — add element, maintains insertion order
 *   2. set.remove(val) — remove element
 *   3. set.contains(val) — check if element exists
 *   4. set.size() — number of elements
 *   5. set.isEmpty() — check if empty
 *   6. set.clear() — remove all elements
 *   7. set.addAll(collection) — union, preserves order
 *   8. set.retainAll(collection) — intersection
 *   9. set.removeAll(collection) — difference
 *   10. set.containsAll(collection) — check if all elements exist
 *   11. set.removeIf(lambda) — remove elements matching condition
 *   12. set.forEach(lambda) — loop in insertion order
 *   13. set.iterator() — iterate in insertion order
 *   14. set.stream() — convert to Stream
 *   15. set.toArray() — convert to array
 */
public class LinkedHashSets {
}