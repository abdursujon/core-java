package ArrayDeque;

/**
 *   --- Stack operations (LIFO) ---
 *   1. deque.push(val) — add to front (top of stack)
 *   2. deque.pop() — remove from front (top of stack)
 *   3. deque.peek() — view front without removing
 *
 *   --- Queue operations (FIFO) ---
 *   4. deque.offer(val) — add to back
 *   5. deque.poll() — remove from front
 *   6. deque.peek() — view front without removing
 *
 *   --- Double-ended operations ---
 *   7. deque.addFirst(val) — add to front
 *   8. deque.addLast(val) — add to back
 *   9. deque.removeFirst() — remove from front
 *   10. deque.removeLast() — remove from back
 *   11. deque.peekFirst() — view front without removing
 *   12. deque.peekLast() — view back without removing
 *   13. deque.offerFirst(val) — add to front (returns false if fails)
 *   14. deque.offerLast(val) — add to back (returns false if fails)
 *   15. deque.pollFirst() — remove from front (returns null if empty)
 *   16. deque.pollLast() — remove from back (returns null if empty)
 *
 *   --- General ---
 *   17. deque.size() — number of elements
 *   18. deque.isEmpty() — check if empty
 *   19. deque.contains(val) — check if element exists
 *   20. deque.clear() — remove all elements
 *   21. deque.iterator() — get an iterator
 *   22. deque.forEach(lambda) — loop through elements
 *   23. deque.stream() — convert to Stream
 */
public class ArrayDeques {
}