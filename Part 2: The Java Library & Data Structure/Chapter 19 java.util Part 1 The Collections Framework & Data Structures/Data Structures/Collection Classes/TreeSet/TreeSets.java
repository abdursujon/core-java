package TreeSet;

/**
 *   --- Core ---
 *   1. set.add(val) — add element (maintained in sorted order)
 *   2. set.remove(val) — remove element
 *   3. set.contains(val) — check if element exists
 *   4. set.size() — number of elements
 *   5. set.isEmpty() — check if empty
 *   6. set.clear() — remove all elements
 *
 *   --- Sorted/Navigable operations ---
 *   7. set.first() — smallest element
 *   8. set.last() — largest element
 *   9. set.lower(val) — greatest element strictly less than val
 *   10. set.higher(val) — smallest element strictly greater than val
 *   11. set.floor(val) — greatest element less than or equal to val
 *   12. set.ceiling(val) — smallest element greater than or equal to val
 *   13. set.subSet(from, to) — elements between from (inclusive) and to (exclusive)
 *   14. set.headSet(val) — elements less than val
 *   15. set.tailSet(val) — elements greater than or equal to val
 *   16. set.descendingSet() — reverse order view
 *   17. set.pollFirst() — remove and return smallest
 *   18. set.pollLast() — remove and return largest
 *
 *   --- General ---
 *   19. set.addAll(collection) — add all elements
 *   20. set.retainAll(collection) — intersection
 *   21. set.removeAll(collection) — difference
 *   22. set.forEach(lambda) — loop in sorted order
 *   23. set.iterator() — iterate in sorted order
 *   24. set.stream() — convert to Stream
 *   25. new TreeSet<>(Comparator) — custom sort order
 */
public class TreeSets {
}