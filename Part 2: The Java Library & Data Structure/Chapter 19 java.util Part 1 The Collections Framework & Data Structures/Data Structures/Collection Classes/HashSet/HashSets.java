package HashSet;

/**
 *   1. set.add(val) — add element (returns false if duplicate)
 *   2. set.remove(val) — remove element
 *   3. set.contains(val) — check if element exists
 *   4. set.size() — number of elements
 *   5. set.isEmpty() — check if empty
 *   6. set.clear() — remove all elements
 *   7. set.addAll(collection) — add all elements from another collection (union)
 *   8. set.retainAll(collection) — keep only elements in both (intersection)
 *   9. set.removeAll(collection) — remove all elements found in another collection (difference)
 *   10. set.containsAll(collection) — check if all elements of another collection exist
 *   11. set.removeIf(lambda) — remove elements matching condition
 *   12. set.forEach(lambda) — loop through elements
 *   13. set.iterator() — get an iterator
 *   14. set.stream() — convert to Stream
 *   15. set.toArray() — convert to array
 */
public class HashSets {
}
