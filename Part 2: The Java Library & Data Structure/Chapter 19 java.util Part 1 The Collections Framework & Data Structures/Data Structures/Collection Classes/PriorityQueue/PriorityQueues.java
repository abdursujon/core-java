package PriorityQueue;

/**
 *   1. pq.offer(val) — add element (placed by priority)
 *   2. pq.poll() — remove and return highest priority element (smallest by default)
 *   3. pq.peek() — view highest priority element without removing
 *   4. pq.add(val) — same as offer (throws exception if fails)
 *   5. pq.remove() — same as poll (throws exception if empty)
 *   6. pq.remove(val) — remove specific element
 *   7. pq.contains(val) — check if element exists
 *   8. pq.size() — number of elements
 *   9. pq.isEmpty() — check if empty
 *   10. pq.clear() — remove all elements
 *   11. pq.toArray() — convert to array
 *   12. pq.iterator() — get an iterator (no guaranteed order)
 *   13. pq.forEach(lambda) — loop through elements (no guaranteed order)
 *   14. pq.stream() — convert to Stream
 *   15. new PriorityQueue<>(Comparator.reverseOrder()) — max-heap instead of min-heap
 *   16. new PriorityQueue<>(collection) — create from existing collection
 */
public class PriorityQueues {
}